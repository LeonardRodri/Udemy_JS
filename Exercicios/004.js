function div(dividendo, divisor) {
    const result = dividendo / divisor
    const resto = dividendo % divisor 

    console.log(`O resulta e ${result} e resto da divisão e ${resto}`)
}

console.log(div(10,2))